# Dados brutos

Os extratores salvam caches regeneráveis em `data/raw/{fonte}_{indicador}.csv`. Esses CSVs são ignorados pelo Git por padrão para evitar versionar grandes downloads e dados que podem ser obtidos novamente das fontes.
