# Arquivos processados

Esta pasta é preenchida por `python run_pipeline.py --stage all`.

Arquivos esperados:

- `painel_governos.parquet`
- `painel_governos.json`
- `resumo_estatistico_por_governo.csv`
- `resumo_estatistico_longo.csv`

Os arquivos não são pré-preenchidos com números de demonstração para evitar confusão entre mock do dashboard e dados extraídos das fontes oficiais.
